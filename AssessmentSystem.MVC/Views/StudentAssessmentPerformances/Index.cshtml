@model IEnumerable<AssessmentSystem.MVC.StudentAssessmentPerformance>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Grade)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RegisterDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StudentAssessment.StudentAssessmentId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Grade)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RegisterDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StudentAssessment.StudentAssessmentId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StudentAssessmentPerformanceId }) |
            @Html.ActionLink("Details", "Details", new { id=item.StudentAssessmentPerformanceId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StudentAssessmentPerformanceId })
        </td>
    </tr>
}

</table>
